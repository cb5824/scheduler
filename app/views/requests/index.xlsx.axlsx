wb = xlsx_package.workbook

wb.add_worksheet(name: "Requests") do |sheet|
  sheet.add_row ["Job Title", "Description"]
  @requests.each do |request|
    sheet.add_row [request.title, request.description]
  end
end
